export default function SpecialistDashboard() {
  return (
    <div className="min-h-screen flex items-center justify-center px-6" style={{ background: "hsl(var(--background))" }}>
      <div className="text-center space-y-4">
        <h1 className="text-3xl font-heading font-semibold" style={{ color: "hsl(var(--primary))" }}>
          Welcome to PULZ
        </h1>
        <p className="text-muted-foreground">Your specialist dashboard is coming soon.</p>
      </div>
    </div>
  );
}
